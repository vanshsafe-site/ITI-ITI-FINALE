# Copy this to .env.local for local dev, and also add these in
# Vercel Dashboard -> Project -> Settings -> Environment Variables.

# Server-side only (used inside api/astro-chart.ts). Do NOT prefix with
# VITE_ / NEXT_PUBLIC_ / etc. — it must stay invisible to the browser.
# Get a free key at https://www.navamsha.in/auth/signup (10,000 calls/month, no card).
NAVAMSHA_API_KEY=your_navamsha_api_key_here
